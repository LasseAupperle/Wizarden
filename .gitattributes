# Normalize line endings: store text as LF in the repo (CI/Render/Netlify are Linux).
* text=auto eol=lf

# Binary assets — never normalize.
*.wav binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.woff binary
*.woff2 binary
