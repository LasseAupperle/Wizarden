# Client build env (Vite). Copy to .env.local for local dev.
# The backend URL — Socket.IO connects here. In prod = the Render service URL.
VITE_SERVER_URL=http://localhost:3001
# "true" enables debug-only UI (bots, 2-seat start). Keep "false" in prod.
VITE_ENABLE_DEBUG=true
